# Core runtime dependencies for edl-panel.
# These are already provided by edx-platform at runtime; pinned loosely here
# so the plugin can be installed and tested standalone.
Django
djangorestframework
